[project]
name = "aar-admin-web"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "Django==5.2.9",
    "httpx>=0.28.0",
    "gunicorn>=23.0.0",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = ["pytest", "pytest-django"]
